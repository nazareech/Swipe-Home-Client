package com.nazar_protasov.swipehome.ui.theme

import androidx.compose.ui.graphics.Color

// Основний фірмовий зелений колір SwipeHome (для кнопок, іконок, активних станів)
val PrimaryGreen = Color(0xFF00665E)

// Світлий відтінок зеленого (може знадобитися для фону виділених елементів)
val SecondaryGreen = Color(0xFFE5F0EF)

// Кольори фону та поверхонь
val BackgroundLight = Color(0xFFF8F9FA) // Дуже світлий сірий для загального фону
val SurfaceWhite = Color(0xFFFFFFFF) // Чистий білий для карток нерухомості

// Кольори тексту
val TextPrimary = Color(0xFF1A1A1A) // Темно-сірий/майже чорний для заголовків
val TextSecondary = Color(0xFF757575) // Світліший сірий для підписів (напр. "Ще немає акаунта?")

// Додатковий колір для сірих плашок
val SurfaceGray = Color(0xFFF3F4F6)

// Системні кольори
val ErrorRed = Color(0xFFD32F2F) // Червоний для повідомлень про помилки вводу
